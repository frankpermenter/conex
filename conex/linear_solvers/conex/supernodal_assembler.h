#pragma once
// This header previously contained BuildBlock() helpers for the supernodal
// solver path.  Those are no longer used in the tree-solver-only build.
// The header is kept (empty) to avoid breaking any residual #include
// directives.
